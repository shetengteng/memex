# Memex v4 功能点开发清单

> 日期：2026-05-31
> 来源：`20260531-03-Memex-v4最终设计文档.md`
> 视角：**功能模块视角**（按"采集 / 处理 / 存储 / 检索 / 暴露 / UI / 运维"组织）。
> 与执行计划的关系：`20260531-13-Memex-执行计划-Rust.md` 是**时间视角（Sprint 排期）**，本文是**功能视角（模块全集）**，二者互补。
> 完成一项打 `[x]`，未完成保持 `[ ]`。

---

## 1. Collector（采集）

### 1.1 Claude Code Adapter
- [x] 扫描 `~/.claude/projects/` 发现项目目录
- [x] 解析 `<session-uuid>.jsonl` 消息格式（serde_json 逐行）
- [x] 按 mtime + offset 增量读取
- [x] session 边界判定（同文件 = 同 session）
- [ ] private session 标记过滤
- [x] 记录扫描状态到 `sources` 表

### 1.2 Cursor Adapter
- [x] 路径：Cursor agent-transcripts JSONL（实际采集路径）
- [ ] 只读 SQLite → 提取 composerData JSON blob（当前采集 agent-transcripts JSONL）
- [ ] 缺 Full Disk Access 权限时友好报错
- [x] 增量追踪（基于 offset）

### 1.3 Codex Adapter
- [x] 扫描 `~/.codex/session_index.jsonl` + date-based glob 定位 session 文件
- [x] 解析 Codex 自有 JSONL 格式（参考 tars-ai-butler）
- [x] 增量 offset 记录

### 1.4 OpenCode Adapter
- [x] 调研 `~/.opencode/sessions/*.json` 实际格式
- [x] 实现 OpenCode adapter

### 1.5 Adapter 通用能力
- [x] adapter 错误隔离：单 adapter 失败不影响其他
- [x] 路径不存在 / 空目录 → 返回空 + 友好提示
- [x] adapter 开关（`config.toml` 配置）
- [ ] `~/.memex/redactions.yaml` 中支持 path / keyword 黑名单作为 private session 判定

---

## 2. Processor（处理）

### 2.1 Normalize
- [x] 统一不同 adapter 的 message 字段为 envelope（role / content / timestamp / tool_call）

### 2.2 Chunk 切分
- [x] 按 Markdown 标题 / 段落 / 列表切分
- [x] 代码块独立 chunk（识别 language）
- [x] tool call 独立 chunk
- [x] tool result 与长日志按结构切分
- [ ] assistant 长回答按"解释 / 方案 / 代码 / 结论"拆分
- [x] 长段落按 ~500 token 切分（MAX_CHUNK_CHARS=2000）
- [x] chunk 保留 `message_id` 回溯完整上下文

### 2.3 Redaction（脱敏）
- [x] 内置规则：API key / email / phone / IP / 银行卡 / 密码上下文 / token 上下文
- [x] 替换格式：`[REDACTED:<type>]`
- [ ] `redactions.yaml` 自定义规则加载
- [ ] redactions 表记录所有命中
- [ ] 云端 LLM 调用前必须先 redact

### 2.4 Metadata 抽取
- [x] topics 抽取（关键词）
- [x] languages 抽取（代码块语言）
- [x] has_code 标记
- [x] tools_used 抽取（Read / Write / Bash 等）
- [x] error_keywords 抽取（Error / panic / Exception）

### 2.5 Summarize（增强，依赖 LLM）
- [ ] L1 chunk 摘要（短，单条 chunk 一句话）
- [ ] L2 session 摘要（结构化要点）
- [ ] L3 project 摘要
- [ ] L4 周期摘要（日报 / 周报，参考 tars 周期报告模板）
- [ ] LLM 失败时不阻塞确定性步骤入库

---

## 3. Storage（存储）

### 3.1 Markdown 真源
- [x] 每个 session 一个 `.md` 文件（YAML frontmatter + role 分段）
- [x] frontmatter 含：session_id / source / project / created
- [x] 追加消息到已有文件
- [x] 写失败回滚 → offset 不更新

### 3.2 SQLite 索引
- [x] schema 初始化（`sources` / `sessions` / `messages` / `chunks` / `access_log` / `kv`，待补 `summaries` / `tags` / `session_tags` / `redactions` / `metrics`）
- [x] WAL 模式
- [x] FTS5 虚表 `chunks_fts` + INSERT/DELETE/UPDATE trigger
- [x] schema migration 框架（版本号）
- [x] 内容 hash 幂等去重（blake3）

### 3.3 写入顺序
- [x] Markdown 先写 → SQLite 后写 → offset 最后更新
- [x] 任何一步失败都不破坏可重放性

### 3.4 重建与备份
- [x] `memex rebuild-index` 从 Markdown 重建 SQLite
- [x] `memex backup <path>` 导出 tar.gz
- [ ] 备份恢复后搜索结果与原库一致

---

## 4. Retriever（检索）

- [x] FTS5 BM25 chunk 级检索
- [x] snippet 截取（前后 N 字符 + 高亮标记，snippet 函数）
- [x] `match_reason` 生成（命中关键词 / chunk_type / source）
- [x] 过滤器：adapter / project / session / 时间范围 / chunk_type
- [x] 排序：BM25 + recency 时间衰减
- [x] chunk 结果 join 回 message / session（FTS JOIN sessions + messages）
- [ ] 中文分词方案对比（unicode61 vs jieba-rs vs bigram）→ 落地最佳方案

---

## 5. CLI

### 5.1 命令
- [x] `memex ingest [--adapter <name>]`
- [x] `memex search <query> [--json] [--filter ...]`（支持 --adapter / --project / --chunk-type / --after / --before）
- [x] `memex sessions [--recent N] [--days N]`
- [x] `memex session <id>`（支持前缀匹配）
- [x] `memex stats`
- [x] `memex config show / set <key> <value>`
- [x] `memex backup <path>`
- [x] `memex rebuild-index`
- [x] `memex doctor`
- [ ] `memex daemon start / stop / status`
- [ ] `memex setup cursor / claude-code`（自动写 MCP 配置）
- [x] `memex mcp`（进入 MCP stdio 模式）

### 5.2 通用能力
- [x] 全局 `--json` 输出契约稳定
- [ ] CLI stderr 与 exit code 与 JSON 契约文档化
- [ ] CLI 自动检测 daemon：在线走 HTTP，离线直读 SQLite

---

## 6. MCP（Rust 内置）

- [x] 选型：手写 stdio JSON-RPC（轻量，无外部依赖）
- [x] `memex mcp` 子命令进入 stdio server
- [x] tool：`search_memory`
- [x] tool：`get_session`
- [x] tool：`list_recent`
- [x] tool：`stats`
- [x] 错误码 + JSON-RPC error 格式
- [x] 直接调用 `memex-core`，不 spawn 子进程
- [ ] private session 永远不出现在 MCP 结果
- [ ] 启动到首次响应 < 25ms（性能验证）

---

## 7. Daemon + HTTP API

- [ ] `crates/memex-daemon` 基于 axum + tokio
- [ ] HTTP 仅监听 `127.0.0.1:9999`
- [ ] API：`/health` / `/search` / `/sessions` / `/sessions/{id}` / `/stats` / `/config`
- [ ] graceful shutdown + signal handling
- [ ] notify crate 文件监听各 adapter 数据源
- [ ] 文件变更 → debounce 2s → 自动 ingest
- [ ] daemon 挂掉时 CLI 自动 fallback SQLite
- [ ] `~/.memex/daemon.lock` 写入 PID + 端口
- [ ] launchd plist 自动生成
- [ ] 日志轮转（保留 7 天 / 最大 50MB）

---

## 8. Tauri Menubar

### 8.1 框架
- [x] Tauri 2 + Vue 3 + TypeScript + shadcn-vue
- [x] 搬运 DiskMind 的 oklch CSS token + Vue 组件结构
- [x] menubar 模式（无 dock icon）

### 8.2 Tray 状态
- [x] 5 种状态图标：healthy / collecting / warning / paused / private
- [x] tray 右键菜单：Today stats / Recent / Search / Settings / Quit

### 8.3 Popup 视图（参考 `20260531-09` v3 单 popup 模型）
- [x] 搜索视图（输入框 + chunk 结果列表 + snippet 高亮）
- [x] 状态视图（今日统计 + 最近 session）
- [x] 设置视图（adapter 开关 + LLM 配置 + 隐私选项）
- [x] 全键盘可达 + Esc 关闭
- [x] ⌘⇧M 全局快捷键唤起

### 8.4 Tauri IPC
- [x] `get_stats`
- [x] `list_recent`
- [x] `search`
- [x] `get_session`
- [x] `toggle_adapter`

### 8.5 Deep link
- [ ] `memex://session/<id>` 协议注册
- [ ] 链接打开后跳转到对应 session 详情

---

## 9. LLM 集成

- [ ] LLM provider trait（`async_trait::summarize / embed`）
- [ ] Ollama provider（reqwest + streaming，从 DiskMind 搬运）
- [ ] Anthropic provider（reqwest + API key 管理，从 DiskMind 搬运）
- [ ] provider 选择逻辑：本地优先 → Claude opt-in 兜底
- [ ] `credentials.toml` 600 权限 + 环境变量 fallback
- [ ] Claude fallback 开启前展示上传范围说明
- [ ] 云端调用前必须先 redact

---

## 10. 横向质量

### 10.1 隐私与安全
- [ ] 默认不上传任何会话内容到云端
- [ ] Claude fallback 必须显式 opt-in
- [ ] 云端调用前必须 redact
- [ ] 配置文件不明文存储 API key
- [ ] HTTP server 仅监听 `127.0.0.1`
- [ ] private session 不进入 MCP 搜索

### 10.2 可观测性
- [ ] 每次搜索写入 `access_log`
- [ ] 每日调用统计写入 `metrics`
- [ ] 慢查询 > 500ms 记录 warning
- [ ] adapter 解析失败记录错误计数
- [ ] `memex doctor` 输出 adapter / SQLite / daemon / MCP / LLM 状态

### 10.3 性能目标
- [ ] FTS5 搜索（1000 chunks）< 50ms
- [ ] FTS5 搜索（10000 chunks）< 200ms
- [ ] CLI 冷启动 < 25ms
- [ ] menubar popup 打开 < 100ms
- [ ] rebuild-index（1000 sessions）< 30s

### 10.4 测试（详见 `20260531-14-Memex-单元测试用例设计.md` / `20260531-15-Memex-测试TODO.md`）
- [ ] adapter fixture 测试
- [ ] redaction 单元测试
- [ ] Markdown schema round-trip 测试
- [ ] SQLite migration 测试
- [ ] FTS5 chunk 检索测试
- [ ] CLI JSON contract 测试（snapshot）
- [ ] MCP tool contract 测试
- [ ] daemon fallback 测试
- [ ] Tauri IPC contract 测试

### 10.5 分发
- [ ] Homebrew formula
- [ ] DMG 打包 + 代码签名
- [ ] Tauri updater 自动更新检查

---

## 11. 候选功能（v0.4+）

- [ ] Linux 完整支持
- [ ] Windows tray 支持
- [ ] Web UI 全屏版
- [ ] 第三方 adapter：Aider / Continue / Cline
- [ ] AI 主动日报
- [ ] AI 主动周报
- [ ] 跨 session 智能续写
- [ ] 与 tars-ai-butler 数据互导
- [ ] 团队协作加密同步评估
