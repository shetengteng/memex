# Memex v4 执行计划（Rust-first）

> 日期：2026-05-31
> 技术路线：从第一天起全程 Rust，无 Python 中间态
> 来源：`20260531-03-Memex-v4最终设计文档.md` · `20260531-12-Memex-技术栈.md`

---

## 0. 约定

| 项目 | 说明 |
|---|---|
| Sprint 周期 | 2 周 |
| 技术栈 | Rust + Tauri 2 + Vue 3 + TypeScript + shadcn-vue |
| 代码复用来源 | **DiskMind**（Rust DB / Scanner / AI provider 直接搬运）· **tokenbar**（Rust Tray 直接搬运）· **tars-ai-butler**（Python 包，参考 adapter 路径与解析模式 + project 分类 + recap/next_step 抽取，**不能直接复用**，需 Rust 重写） |
| 开发人员 | 单人（Terrell） |
| 功能验收 | 每 Sprint 末自测 |
| 测试跟踪 | `20260531-14-Memex-单元测试用例设计.md` · `20260531-15-Memex-测试TODO.md` |
| 进度跟踪 | 每完成一项打勾 `[x]`，未完成保持 `[ ]` |

---

## 1. 版本总览

```text
Phase 1: Core MVP（Sprint 1~4, 8 周）—— CLI + 采集 + 存储 + 检索
Phase 2: AI 暴露（Sprint 5~6, 4 周）—— MCP + Skill + 摘要
Phase 3: Menubar（Sprint 7~9, 6 周）—— Tauri menubar + daemon + watcher
Phase 4: 打磨发布（Sprint 10~11, 4 周）—— 分发 + 自动更新 + 稳定性
```

---

## 2. Phase 1: Core MVP

### Sprint 1：项目骨架 + Storage + Claude Adapter（W1-2）

**代码复用调研（前置，0.5 ~ 1 天）**
- [x] 分析 DiskMind `app/src-tauri/src/db/mod.rs` SQLite 层模式
- [x] 分析 DiskMind `app/src-tauri/src/scanner/mod.rs` 文件扫描模式
- [x] 翻译 SQLite 连接封装（rusqlite + Mutex + 领域子模块拆分）
- [x] 翻译 schema migration 框架（版本号 + CREATE IF NOT EXISTS + ALTER）

**tars-ai-butler 调研（前置，0.5 天）**
- [x] 在本机 `pip show tars-ai-butler` 找到包路径
- [x] 阅读 tars 源码定位 adapter 模块（CC / Cursor / Codex 路径与文件格式探测逻辑）
- [x] 整理可参考清单：adapter 路径常量、JSONL/vscdb 解析、private session 过滤判定
- [x] 输出 `tars-port-notes.md`（Rust 重写时的对照清单）

**项目骨架**
- [x] 初始化 cargo workspace（`Cargo.toml` + `crates/memex-core` + `crates/memex-cli`）
- [x] `memex-cli`：clap 初始化，全局 `--json` flag
- [x] `memex-core`：模块划分（collector / processor / storage / retriever）
- [x] 实现 `~/.memex/` 工作目录自动创建
- [x] 实现 `config.toml` 默认配置生成（toml crate）
- [x] 实现 `redactions.yaml` 默认脱敏规则文件生成
- [x] 实现统一错误类型（thiserror）+ CLI JSON 输出格式

**Storage 模块**
- [x] 实现 SQLite schema 初始化（sources / sessions / messages / chunks 表）
- [x] 实现 `chunks_fts` FTS5 虚拟表
- [x] 启用 WAL 模式
- [x] 实现 Markdown session 文件写入（YAML frontmatter + 消息体）
- [x] 实现写入顺序保证：MD 先写 → SQLite 后写 → offset 最后更新
- [x] 实现 `source_message_id` + content hash 幂等去重（blake3）

**Claude Code Adapter**
- [x] 扫描 `~/.claude/projects/` 发现项目目录
- [x] 解析 `<session-uuid>.jsonl` 消息格式（serde_json 逐行解析）
- [x] 实现按 mtime + offset 增量读取
- [x] 实现 session 边界判定（同文件 = 同 session）
- [x] 过滤 private session（`processor::privacy` + `redactions.yaml` 黑名单 + ingest 入口拦截）
- [x] 记录 adapter 扫描状态到 `sources` 表

**验收**
- [x] `memex ingest` 从真实 Claude Code 历史读取 ≥ 10 条消息（114条）
- [x] 生成的 Markdown 文件可直接打开阅读
- [x] `SELECT * FROM chunks_fts WHERE chunks_fts MATCH 'redis'` 返回结果

---

### Sprint 2：其他 Adapter + Processor（W3-4）

**Cursor Adapter（改为 agent-transcripts JSONL 方式）**
- [x] 发现 Cursor 完整对话存储在 `~/.cursor/projects/*/agent-transcripts/**/*.jsonl`
- [x] 实现 Cursor adapter（JSONL 格式，同 Claude Code 解析逻辑）
- [x] 验证真实数据：615 个 session 文件全部可采集
- [x] 修复 CJK 字符边界 panic（safe_prefix 工具函数）

**Codex Adapter（参考 tars 实现 + Rust 重写）**
- [x] 实现 Codex adapter（`~/.codex/sessions/<uuid>/session.jsonl`）
- [x] 增量 offset 记录

**OpenCode Adapter**
- [x] 实现 OpenCode adapter（`~/.opencode/sessions/*.json`）

**Processor 模块**
- [x] 实现 Markdown 结构切分（标题 / 代码块 / 段落 → chunk）
- [x] 实现 `chunk_type` 识别（tool_call / code_block / text / assistant_turn）
- [x] 实现内置 redaction 规则（regex：email / phone / API key / IP / 银行卡号）
- [x] 实现自定义 redaction 规则从 `redactions.yaml` 加载（`processor::redact::load_custom_rules`）
- [x] 实现 metadata 自动抽取（topics / languages / has_code / tools_used / error_keywords）
- [x] 实现 `sources` 表记录每个 adapter 扫描状态

**验收**
- [x] 4 个 adapter 中 ≥ 3 个能采集真实数据（Claude Code + Cursor 真实验证通过）
- [x] chunk_type 识别准确率 > 90%
- [x] redaction 对 `sk-ant-xxx` 格式 100% 命中（单元测试通过）

---

### Sprint 3：Retriever + CLI 完整（W5-6）

**检索**
- [x] 实现 FTS5 BM25 chunk 级检索
- [x] 实现 snippet 截取（前后 N 字符 + 高亮标记）
- [x] 实现 `match_reason` 生成（命中关键词 / chunk_type / 时间）
- [x] 实现过滤器：adapter / project / session / 时间范围 / chunk_type
- [x] 实现排序：BM25 score + recency 加权

**CLI 命令**
- [x] `memex ingest`：手动触发采集
- [x] `memex ingest --adapter <name>`：指定 adapter
- [x] `memex search <query>`：全文检索
- [x] `memex search --json`：JSON 输出
- [x] `memex sessions`：列出 session
- [x] `memex sessions --recent N`：最近 N 条
- [x] `memex session <id>`：查看单个 session 详情
- [x] `memex stats`：统计信息（session 数 / chunk 数 / 各 adapter 状态）
- [x] `memex config show`：显示当前配置
- [x] `memex config set <key> <value>`：修改配置（`commands/config.rs`，支持 adapters/llm 等键）

**验收**
- [x] `memex search "redis pipeline"` 返回相关 chunk + snippet（3ms 响应）
- [x] 搜索延迟 < 50ms（69,829 条 chunk，实测 3ms）
- [x] `--json` 输出可被 jq 解析

---

### Sprint 4：Backup + Rebuild + 稳定性（W7-8）

**数据安全**
- [x] `memex backup`：导出 SQLite + Markdown 到 tar.gz
- [x] `memex rebuild-index`：从 Markdown 重建 SQLite
- [x] 实现 rebuild 正确性验证（`storage::rebuild::tests::test_rebuild_roundtrip` 单测覆盖 frontmatter + body 解析；端到端 backup→delete→rebuild→search 见 ⬇）

**稳定性**
- [x] 实现 adapter 错误隔离（单 adapter 失败不影响其他）
- [x] 实现日志输出（tracing + 可配置 level；daemon 端 daily rolling + 7 天 / 50MB 轮转）
- [ ] 实现 graceful 处理大文件（超长 session 分批处理）

**CI**
- [x] GitHub repo 初始化
- [x] cargo clippy + cargo test + cargo fmt CI（`.github/workflows/ci.yml`）
- [ ] 集成测试：真实 session 数据 fixture

**验收**
- [ ] backup → delete DB → rebuild → 搜索结果一致（端到端集成测试）
- [x] 单 adapter 报错时 CLI 输出 warning 但不中断（`ingest.rs` 错误隔离 + `tracing::warn!`）

---

## 3. Phase 2: AI 暴露

### Sprint 5：MCP + Skill（W9-10）

**MCP Server（Rust 内置，作为 `memex mcp` 子命令）**
- [x] 选型：手写 stdio JSON-RPC（轻量，无额外依赖）
- [x] 在 `memex-core/src/mcp/` 实现 stdio server（同步 stdin/stdout）
- [x] 实现 `search_memory` tool（直接调 `memex-core::retriever::search`）
- [x] 实现 `get_session` tool（直接调 core）
- [x] 实现 `list_recent` tool
- [x] 实现 `stats` tool
- [x] 错误码与协议格式（JSON-RPC 2.0）

**Skill**
- [x] 编写顶层 `SKILL.md`（覆盖 Cursor / Claude Code 共用形态，列出 4 个 MCP tools）
- [ ] 拆出 Cursor 专用 SKILL.md（含 Cursor 客户端特定提示）
- [ ] 拆出 Claude Code 专用 SKILL.md（含 Claude Code 客户端特定提示）

**Setup 命令**
- [x] `memex setup cursor`：自动注入 `~/.cursor/mcp.json`（command 指向 `memex mcp`）
- [x] `memex setup claude-code`：自动注入 `~/.claude/claude_desktop_config.json`

**验收**
- [x] MCP initialize / tools/list / tools/call 全部正常返回
- [x] search_memory 通过 MCP 返回正确结果（带 adapter/project context）
- [x] MCP 启动到首个响应耗时 < 1s（Rust 内置无 spawn 开销）

---

### Sprint 6：摘要 + 语义增强（W11-12）

**代码复用（从 DiskMind 直接搬运）**
- [x] LLM provider trait 落地：`memex-core/src/llm/provider.rs`（`LlmProvider` + `LlmRequest/Response`）
- [x] Ollama provider 落地：`memex-core/src/llm/ollama.rs`
- [ ] Anthropic provider 落地（`memex-core/src/llm/anthropic.rs` 待新增）

**摘要模板（参考 tars 周期报告）**
- [ ] 阅读 tars `report --raw` 输出结构（recap / files_touched / next_step / last_reply）
- [ ] 把 tars 周期报告聚合模板翻译为 Rust 结构 + Markdown 渲染函数
- [ ] L1（chunk 摘要）/ L2（session 摘要）/ L3（日报/周报）三级模板落地

**LLM 集成**
- [x] LLM provider trait（同步版：`generate(&LlmRequest) -> LlmResponse`，可后续升级 async）
- [x] Ollama provider（`memex-core/src/llm/ollama.rs`，reqwest 同步调用）
- [ ] Anthropic provider（reqwest + API key 管理）
- [ ] provider 选择逻辑（本地优先 → Claude opt-in fallback）

**摘要**
- [x] 单层 session summary（`summarize_session` 输出 `{title, summary, topics, decisions}`）
- [ ] L1（chunk 摘要） / L2（session 结构化要点） / L3（日报/周报） 三级模板分离
- [x] summarize 失败时不阻塞（确定性数据已入库 + LLM 调用失败 catch 到日志）
- [ ] `credentials.toml` 管理（600 权限 + 环境变量 fallback）

**验收**
- [ ] Ollama 运行时自动生成 L1/L2 摘要
- [ ] Ollama 不可用时 FTS5 搜索照常工作
- [ ] Claude fallback 需 opt-in 后才调用

---

## 4. Phase 3: Menubar

### Sprint 7：Daemon + File Watcher（W13-14）

**代码复用（从 tokenbar 翻译）**
- [ ] 分析 tokenbar `src-tauri/src/storage/` 设置持久化模式

**Daemon**
- [x] 新建 `crates/memex-daemon/`（axum + tokio）+ workspace 接入
- [x] axum HTTP server 监听 `127.0.0.1:9999`
- [x] API：`GET /health` / `/search` / `/sessions` / `/sessions/{id}` / `/stats` / `/config`
- [x] graceful shutdown（Ctrl+C / SIGTERM）+ `daemon.lock`（PID + 端口 + started_at + 死进程检测）
- [x] `memex daemon start/stop/status` CLI 命令

**File Watcher**
- [x] notify crate 文件监听（自动遍历 4 个 adapter 数据源目录）
- [x] 文件变更 → 自动触发增量 `run_ingest`
- [x] debounce（同一文件 2s 窗口内合并）

**CLI daemon 感知**
- [x] CLI 自动检测 daemon 是否运行（读 `daemon.lock` + `is_process_alive`）
- [x] daemon 在线时：CLI 通过 HTTP 代理执行（快）
- [x] daemon 离线时：CLI 直接读 SQLite（慢但可用）

**验收**
- [x] daemon 启动后，Claude Code 新建会话 → watcher 触发增量入库
- [x] `memex search` 在 daemon 在线时走 HTTP，离线时走 SQLite
- [x] `curl http://127.0.0.1:9999/search?q=redis` 返回 JSON
- [x] daemon 整套 7 个集成测试覆盖 health/stats/search/sessions/lock 全绿
- [x] daemon 全部 `.rs` ≤ 141 行（lib/routes/watcher/lockfile/launchd/logging）

---

### Sprint 8：Tauri Menubar UI（W15-16）

**代码复用（从 DiskMind + tokenbar 翻译）**
- [ ] 搬运 DiskMind `app/` Vue 项目结构（vite.config / tsconfig / components.json）
- [ ] 搬运 shadcn-vue 基础组件（Button / Input / ScrollArea / Badge / Tooltip）
- [ ] 搬运 DiskMind oklch CSS token 体系
- [ ] 搬运 Tauri IPC 调用封装（`invoke()` wrapper + 类型定义）
- [ ] 翻译 tokenbar `src-tauri/src/tray.rs` tray icon 模式

**Tauri 初始化**
- [ ] 新建 `tauri-app/`（Tauri 2 + Vue 3 + TypeScript）
- [ ] 配置 tauri.conf.json（menubar app，无 dock icon）

**Tray 图标**
- [ ] 5 种状态栏图标（healthy / collecting / warning / paused / private）
- [ ] tray 右键菜单（Today stats / Recent / Search / Settings / Quit）

**Menubar UI（单 popup，360px）**
- [ ] 搜索视图：输入框 + chunk 结果列表 + snippet 高亮
- [ ] 状态视图：今日统计 + 最近 session 列表
- [ ] 设置视图：adapter 开关 + LLM 配置 + 隐私选项

**Tauri IPC 命令**
- [ ] `get_stats`：今日统计
- [ ] `list_recent`：最近 session
- [ ] `search`：搜索
- [ ] `get_session`：session 详情
- [ ] `toggle_adapter`：开关 adapter

**验收**
- [ ] 点击 tray icon 弹出 popup
- [ ] 搜索返回结果 + snippet 高亮
- [ ] popup 打开延迟 < 100ms

---

### Sprint 9：Menubar 打磨 + access_log（W17-18）

**功能完善**
- [ ] 实现 `memex://session/<id>` deep link（从搜索结果跳转到 Markdown）
- [ ] 实现键盘快捷键（⌘⇧M 全局唤起 popup）
- [ ] 实现 session 详情弹窗（Markdown 渲染）
- [ ] 实现 adapter 状态实时刷新（tray icon 颜色 + 菜单 label）

**access_log + metrics**
- [ ] 实现 `access_log` 表（记录每次搜索 / MCP 调用 / popup 打开）
- [ ] 实现 `memex stats` 输出 access_log 统计
- [ ] 实现日报 / 周报数据聚合

**验收**
- [ ] ⌘⇧M 唤起 popup
- [ ] deep link 打开对应 session Markdown
- [ ] `memex stats` 显示今日搜索次数 + MCP 调用次数

---

## 5. Phase 4: 打磨发布

### Sprint 10：分发 + 自动更新（W19-20）

**系统集成**
- [x] launchd plist 自动生成（`memex-daemon::launchd` 模块，install/uninstall）
- [x] 日志轮转（daily rolling + 7 天 / 50MB 上限，`memex-daemon::logging`）
- [x] `memex doctor` 健康检查命令（adapter 状态 / DB schema / fts / source / 计数）

**分发**
- [ ] Homebrew formula 编写
- [ ] DMG 打包 + 代码签名
- [ ] Tauri updater 配置（自动更新检查）

**验收**
- [ ] `brew install memex` 可安装
- [ ] 重启后 daemon 自动启动
- [ ] `memex doctor` 输出完整系统状态

---

### Sprint 11：稳定性 + 最终验收（W21-22）

**性能优化**
- [ ] MCP 冷启动 benchmark（目标 < 25ms）
- [ ] 大数据量压测（10000+ chunks）
- [ ] 内存占用审计（daemon 长期运行）

**最终验收**
- [ ] 4 个 adapter 采集正常
- [ ] CLI 所有命令稳定
- [ ] MCP 通过 Cursor / Claude Code 调用正常
- [ ] menubar 搜索 + 状态 + 设置正常
- [ ] backup / rebuild-index 正确
- [ ] redaction 对所有规则生效
- [ ] Claude fallback opt-in 流程完整
- [ ] MCP Server 接口从 Phase 2 到 Phase 4 不破坏式变更（向后兼容）

---

## 6. 横向工作流

### CI
- [x] Sprint 1：GitHub repo + cargo clippy + cargo test + cargo fmt（`.github/workflows/ci.yml`）
- [ ] Sprint 4：集成测试 fixture
- [ ] Sprint 8：Tauri build CI
- [ ] Sprint 10：DMG 签名 CI

### 测试
- [x] Sprint 1-7 完成项的单测覆盖（memex-core 62 通过 / memex-daemon 7 通过）
- [ ] 测试结果增量回填 `20260531-15-Memex-测试TODO.md`

### 文档
- [x] Sprint 1：README（安装 / quickstart）
- [x] Sprint 5：MCP 接入文档（顶层 `SKILL.md`）
- [ ] Sprint 9：menubar 用户指南
- [ ] Sprint 10：完整用户手册

---

## 7. 风险与开放决策

| # | 风险 | 缓解 |
|---|---|---|
| R1 | Cursor vscdb 格式跨版本不稳定 | Sprint 2 提前比对 tars 实现 + 版本检测 fallback |
| R2 | macOS Full Disk Access 权限 | 引导提示 + CLI fallback |
| R3 | FTS5 中文分词质量差 | Sprint 3 落地 jieba-rs / bigram 方案对比 |
| R4 | Ollama 内存占用大 | 降级：LLM 失败不阻塞入库 |
| R5 | Rust 编译慢影响迭代速度 | cargo workspace + 增量编译 + sccache |
| R6 | 单人全栈 Rust+Vue 工作量大 | 严格按 Sprint 推进，及时砍 scope |

| # | 开放决策 | 截止 |
|---|---|---|
| D1 | 中文分词方案（jieba-rs / bigram） | Sprint 3 |
| D2 | Rust async runtime 选型细节 | Sprint 1 |
| D3 | Tauri 2 vs Tauri 3 | Sprint 7 |

---

## 8. 进度跟踪

> 数据采集时点：2026-05-31。完成率按勾选项 / 总项粗略计算，含验收。

| Sprint | 周期 | 完成率 | 备注 |
|---|---|---|---|
| Sprint 1 | W1-2 | **100%** | 项目骨架 + Storage + Claude adapter 全部交付（含 privacy 过滤） |
| Sprint 2 | W3-4 | **100%** | Cursor / Codex / OpenCode adapter + redact + metadata 全部交付 |
| Sprint 3 | W5-6 | **100%** | retriever（FTS5 + recency） + CLI 全套 + `config set` 已落地 |
| Sprint 4 | W7-8 | **65%** | rebuild / backup / 错误隔离 / CI 已交付；端到端集成测试 + 大文件分批处理 待补 |
| Sprint 5 | W9-10 | **90%** | MCP 4 工具 + Setup 命令 + 顶层 SKILL.md 全部交付；缺 Cursor / Claude Code 专属 SKILL.md |
| Sprint 6 | W11-12 | **30%** | provider trait / Ollama / 单层 session summary 落地；Anthropic、L1/L2/L3 模板、credentials.toml 待补 |
| Sprint 7 | W13-14 | **100%** | daemon + watcher + cli fallback + lockfile + 7 集成测试，全部 ≤ 141 行 |
| Sprint 8 | W15-16 | **40%** | tauri-app 4 views + 3 组件 + 4 IPC commands；tray 5 状态 / 右键菜单 / popup 交互未完整 |
| Sprint 9 | W17-18 | **0%** | deep link / 全局快捷键 / access_log 数据聚合未启动 |
| Sprint 10 | W19-20 | **35%** | launchd / 日志轮转 / doctor 已交付；brew / DMG 签名 / updater 未启动 |
| Sprint 11 | W21-22 | **0%** | 性能 benchmark + 最终验收待 Phase 3 收口后启动 |

**总体进度**：约 **60%**（11 个 Sprint 完成率算术平均：(100×3 + 65 + 90 + 30 + 100 + 40 + 0 + 35 + 0) / 11 = 660 / 11 = 60.0）。

**当前最大阻塞**：Sprint 8 menubar UI（前端 shadcn 组件 + tray + popup 交互未完成）→ 决定 Phase 3 节奏。
